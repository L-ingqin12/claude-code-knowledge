# Memory 知识库

本目录是 `/root/.claude/projects/-root/memory/` 的归档副本。

Claude Code 在每次会话启动时从 memory 目录加载 `.md` 文件作为持久上下文。
`MEMORY.md` 是索引文件，每个 `.md` 文件是一条可回溯的知识记忆。

有关记忆系统的完整说明，参见 [[claude-context-continuity]]。

---
title: 计算机网络八股
aliases: [网络基础, 计网知识, net-fundamentals]
tags: [cs/net, cs]
created: 2026-08-26
updated: 2026-08-26
status: review
source: RFC 与教材共识（TCP 由 RFC 9293 整合、HTTP/1.1=RFC 9112、HTTP/2=9113、HTTP/3/QUIC=9114/9000、TLS1.3=8446、DNS=1034/1035 及后续）；实践层与库内网络子库互链
fetched_at: 2026-08-26
---

# 计算机网络八股

> [!abstract] 定位
> 协议栈主干八股：分层模型、TCP 可靠性与拥塞控制、TLS 握手、HTTP 演进、DNS。运维实战（路由器/代理/排障）在 network 子库——[[参考-网络路由与代理排障]] / [[Network-KB-Home]]，两库互补不重复。

See also: [[CS-KB-Home]] · [[操作系统八股]] · [[高并发系统设计]] · [[参考-VPN代理诊断与优化]]

## 一、分层模型速览

| 层 | PDU | 代表协议 | 关键问题 |
|----|-----|---------|---------|
| 应用 | 报文 | HTTP/DNS/TLS/WebSocket | 语义 |
| 传输 | 段 | TCP/UDP/QUIC(基于UDP) | 端口复用/可靠/拥塞 |
| 网络 | 包 | IP/ICMP/BGP | 寻址路由 |
| 链路 | 帧 | 以太网/WiFi/ARP | 局域网交付 |

## 二、TCP 核心八股

### 建拆连接
- **三次握手** SYN → SYN+ACK → ACK：双方各确认收发能力 + 同步初始序号 ISN（随机化防预测）
- 为何不是两次：防历史重复 SYN 造成脏连接；为何挥手四次：半关闭（FIN 后仍可收数据）
- **TIME_WAIT = 2MSL**：确保最后 ACK 达到+旧报文自然消亡；服务端大量 TIME_WAIT → `tcp_tw_reuse`/端口池/长连接化
- SYN Flood：syncookies 用加密序号免半连接队列占位

### 可靠性三支柱
1. **重传**：快重传（3 个 dup-ACK）优先于超时 RTO；SACK 报告乱序区块
2. **滑动窗口**：流量控制对端 rwnd；发送缓冲堆积即背压信号
3. **拥塞控制**：慢启动→拥塞避免(AIMD)→快恢复；CUBIC 默认（三次函数窗口增长）；BBR 测带宽×RTT 模型绕开丢包假设（Google 论文口径）

> [!tip] 排障映射
> 重传率/RTT 抖动看 `ss -ti`；本库代理链路诊断实操见 [[参考-网络路由与代理排障]]。

## 三、TLS 与 HTTPS

- **TLS 1.3 握手 = 1-RTT**（合并密钥交换+参数协商），0-RTT 恢复会话但有重放风险
- ECDHE 临时密钥→前向安全；证书链校验：签名逐级验至根 + 域名匹配 + 有效期 + 吊销（OCSP 装订优于实时查询）
- SNI 明文泄漏访问域名 → ECH 加密（部署度**待确认**）

## 四、HTTP 演进

| 版本 | 传输 | 关键特性 | 队头阻塞 |
|------|------|---------|---------|
| 1.1 (RFC 9112) | 文本/TCP | keep-alive、管线化(废) | 应用层串行响应 |
| 2 (9113) | 二进制分帧/TCP | 多路复用、HPACK 头压缩、服务器推送(弃用中) | TCP 层丢包全体阻塞 |
| 3 (9114) | QUIC/UDP | 连接迁移(Connection ID)、内置 TLS1.3、0-RTT | 彻底解耦流间阻塞 |

WebSocket：HTTP Upgrade 升级后全双工帧协议——Agent 流式输出的常用通道（对照 [[opencode-pi-base-development-analysis]] SSE/WebSocket 选型节）。

## 五、DNS

递归+迭代两级查询；根→TLD→权威；缓存层级浏览器/系统/resolv.conf 上游。
记录族：A/AAAA/CNAME/MX/TXT(验证)/SRV/NS。
DoH/DoT 加密上游防劫持——代理客户端配置实操见 [[ARCHITECTURE]] 与 [[参考-VPN代理诊断与优化]]。

## 六、待确认项

> ① ECH 全球部署覆盖率动态变化；② BBR v2/v3 公平性争议的最新社区结论；③ QUIC 在企业中间盒环境的穿透成功率统计口径。

## Related

[[CS-KB-Home]] · [[操作系统八股]] · [[高并发系统设计]] · [[Network-KB-Home]] · [[参考-小米路由器API认证与利用]]
